meta {
  name: Opdaterer et eksisterende hotel.
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/api/Hotels/:id
  body: json
  auth: none
}

params:path {
  id: {{hotel-id}}
}

headers {
  Authorization: {{apiKey}}
}

body:json {
  {
    "id": "{{hotel-id}}",
    "name": "Dummy Hotel - Edit",
    "address": "Dummy Street"
  }
}

tests {
  // Test response status
  test("Hotel create returnerer 200", function() {
    expect(200).to.include(res.status);
  });
  
}
