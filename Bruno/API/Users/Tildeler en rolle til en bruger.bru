meta {
  name: Tildeler en rolle til en bruger.
  type: http
  seq: 11
}

put {
  url: {{baseUrl}}/api/Users/:id/role
  body: json
  auth: bearer
}

params:path {
  id: {{user-id}}
}

headers {
  Authorization: {{apiKey}}
}

auth:bearer {
  token: {{JWT-Admin}}
}

body:json {
  {
    "roleId": "2"
  }
}
