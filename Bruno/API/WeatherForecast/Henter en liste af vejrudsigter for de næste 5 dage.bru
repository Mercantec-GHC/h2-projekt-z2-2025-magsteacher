meta {
  name: Henter en liste af vejrudsigter for de næste 5 dage.
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/WeatherForecast
  body: none
  auth: none
}

headers {
  Authorization: {{apiKey}}
}

tests {
  test("Should always return 200 - for all request", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
