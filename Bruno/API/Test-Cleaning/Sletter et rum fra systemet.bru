meta {
  name: Sletter et rum fra systemet.
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/api/Rooms/:id
  body: none
  auth: none
}

params:path {
  id: {{room-id}}
}

headers {
  Authorization: Bearer {{JWT-Admin}}
}

tests {
  // Test response status
  test("Hotel create returnerer 204", function() {
    expect(204).to.include(res.status);
  });
}
