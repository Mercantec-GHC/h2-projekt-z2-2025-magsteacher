meta {
  name: Henter alle hoteller fra systemet.
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/Hotels
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{JWT-Admin}}
}

tests {
  // Test response status
  test("Hotel create returnerer 200", function() {
    expect(200).to.include(res.status);
  });
  
}
